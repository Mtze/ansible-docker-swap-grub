galaxy_info:
  author: Matthias Linhuber
  description: "Configure GRUB to fix dockers - WARNING: No swap limit support"

  role_name: docker_swap_grub
  namespace: mtze
  issue_tracker_url: https://github.com/Mtze/ansible-docker-swap-grub/issues

  license: MIT

  min_ansible_version: 2.1

  platforms:
    - name: Ubuntu
      versions:
        - focal

  galaxy_tags: [grub, docker, swap, limit]

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
